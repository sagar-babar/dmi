#user  nobody;
worker_processes      auto;
worker_rlimit_nofile  100000;

daemon     off;

error_log  logs/nginx-error.log  info;
pid        logs/nginx.pid;

events {
	use                 epoll;
	worker_connections  10240;
	multi_accept        on;
}

include /usr/local/nginx/conf/dmi/api-lb.conf;
